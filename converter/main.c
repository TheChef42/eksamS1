#include <stdio.h>

int main() {
    int x = 0;
    int y = 0;
    int z = 0;

    printf("Type 1 for binary conversion");
    scanf(" %d", &x);
    printf("Type number to be converted from decimal");
    scanf(" %d", &y);
    switch (x) {
        case 1: //binary
            while(x != 0) {

            }
            printf(" %d", y);
            break;


    }


    printf("Hello, World!\n");
    return 0;
}
